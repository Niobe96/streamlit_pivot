# 🏥 KCDW 데이터 분석 어시스턴트 사용 가이드

환영합니다! **KCDW 데이터 분석 어시스턴트**는 복잡한 코딩이나 통계 지식 없이도 누구나 쉽게 의료 데이터를 정제하고 심도 있게 분석할 수 있도록 돕는 대화형 플랫폼입니다.

본 가이드에서는 처음 사용하시는 분들을 위해 **앱 접속부터 최종 결과물 다운로드까지의 전체 흐름**을 단계별로 안내해 드립니다.

---

## 1단계: 데이터 업로드하기

1. 화면 왼쪽(사이드바)에 있는 **"📁 데이터 파일 업로드"** 영역을 찾습니다.
2. 분석하고자 하는 데이터 파일(`.csv` 또는 `.xlsx`)을 드래그 앤 드롭하거나 클릭하여 업로드합니다.
3. 데이터가 성공적으로 업로드되면 메인 화면에 **총 행/열 개수, 수치형/범주형 변수 개수 등 요약 정보**가 챗봇 메시지로 나타납니다.

---

## 2단계: 초기 데이터 설정하기

데이터를 본격적으로 분석하기 전, 데이터의 기준과 형태를 앱에게 알려주는 단계입니다. 

### ① 환자 ID 및 종속변수 지정
* **환자 ID 컬럼:** 데이터를 묶거나 추적할 기준이 되는 고유 식별자(예: `Patient_ID`, `등록번호` 등)를 선택합니다.
* **종속변수 (Target):** 예측하거나 집중적으로 분석하고 싶은 결과값(예: `발병여부`, `생존율` 등)을 선택합니다.
* 종속변수가 범주형(카테고리)인지 수치형인지 체크한 뒤 **"✅ 저장하고 다음 단계로"**를 클릭합니다.

### ② 데이터 타입 최적화 및 변환
* 앱이 데이터를 자동으로 스캔하여 카디널리티(고유값의 개수)가 낮아 **'범주형(Category)'으로 변환하는 것이 유리한 컬럼들을 추천**해 줍니다.
* **"✨ 추천 모두 적용하기"** 버튼을 눌러 일괄 변환하거나, 하단의 데이터 타입 설정창을 통해 수동으로 하나하나 타입을 변경할 수도 있습니다.
* 잘못 변경했다면 스크롤을 내려 **변경 내역(롤백)**에서 언제든지 원상복구할 수 있습니다.
* 완료되었다면 **"✅ 설정 완료 → 분석 시작!"** 버튼을 클릭합니다.

---

## 3단계: 원하는 분석/작업 수행하기

설정이 끝나면 화면 아래에 **기능 버튼들**이 나타납니다. 원하시는 작업 흐름을 선택해 보세요.

### 📊 데이터 펼치기 (1:N 피벗 변환)
한 환자가 여러 개의 기록(혈압, 약 처방 등)을 가지고 있는 **세로로 긴 데이터(1:N)**를, 한 환자당 한 줄이 되도록 **가로로 넓게(1:1)** 펼치는 기능입니다.
* 기준 컬럼(예: 환자 ID)과 펼칠 데이터 컬럼을 선택하고 **"✅ 펼치기 시작"**을 누르면 자동으로 병합됩니다.

### 📈 통계 분석
데이터의 기초적인 분포부터 고급 검정까지 수행할 수 있습니다.
* **🔍 빠른 탐색:** 단일 변수의 분포(히스토그램), 산점도, 박스플롯, 그리고 결측치나 요약 통계를 즉시 확인합니다.
* **🔬 고급 통계/검정:** 두 그룹 간의 차이를 비교하거나(t-검정, ANOVA), 범주형 데이터 간의 연관성(카이제곱 검정), 생존 분석, 회귀분석, 상관계수 히트맵 등 논문 작성에 필요한 전문 통계를 클릭 한 번으로 수행합니다.



---

## 4단계: 분석 결과 확인 및 내보내기

KCDW 어시스턴트는 챗봇 형식으로 동작합니다. 
* 버튼을 눌러 작업을 지시할 때마다 화면 하단에 **새로운 채팅과 함께 분석 결과(표, 그래프)가 부드럽게 스크롤되며** 나타납니다. (자동 스크롤 기능 덕분에 방금 도출된 결과를 바로 확인할 수 있습니다!)
* **👇 이전 대화 기록:** 과거에 수행했던 분석 기록은 스크롤을 올려 언제든지 다시 확인할 수 있습니다.

**결과 저장하기:**
분석이 완료된 표와 그래프 아래에는 항상 다운로드 버튼이 제공됩니다.
* **📥 Excel / CSV 다운로드:** 전처리된 데이터나 통계 결과표를 엑셀 형식으로 내보냅니다.
* **🖼️ PNG 이미지 저장:** 화면에 출력된 시각화 그래프를 고화질 이미지 파일로 저장하여 보고서나 논문에 바로 활용할 수 있습니다.

---

## 💡 유용한 팁 (Tip)

* **다시 설정하고 싶을 때:** 분석 중간에 데이터 타입을 변경하고 싶다면, 현재 화면의 하단에 있는 **"🔁 다시 설정할게"** 버튼을 눌러 설정 단계로 언제든 돌아갈 수 있습니다.
* **처음부터 다시 시작:** 다른 파일을 분석하거나 아예 초기 화면으로 돌아가고 싶다면 언제든 **"🏠 홈으로 가기"** 또는 **"처음으로 돌아갈게"** 버튼을 활용하세요.

지금 바로 파일을 업로드하고 첫 번째 분석을 시작해 보세요! 🎉
