# 🏥 KCDW 데이터 분석 어시스턴트 사용 가이드

환영합니다! **KCDW 데이터 분석 어시스턴트**는 복잡한 코딩이나 통계 지식 없이도 누구나 쉽게 의료 데이터를 정제하고 심도 있게 분석할 수 있도록 돕는 **대화형 분석 플랫폼**입니다.

본 가이드에서는 처음 사용하시는 분들을 위해 **앱 접속부터 최종 결과물 다운로드까지의 전체 흐름**을 단계별로 안내합니다.

---

## 📋 목차

1. [로그인 및 데이터 업로드](#1단계-로그인-및-데이터-업로드)
2. [초기 데이터 설정](#2단계-초기-데이터-설정)
3. [데이터 탐색 (기초 분석)](#3단계-데이터-탐색-기초-분석)
4. [통계 검정 (고급 분석)](#4단계-통계-검정-고급-분석)
5. [데이터 펼치기 (피벗 변환)](#5단계-데이터-펼치기-피벗-변환)
6. [결과 확인 및 내보내기](#6단계-결과-확인-및-내보내기)
7. [유용한 팁](#-유용한-팁)

---

## 1단계: 로그인 및 데이터 업로드

### 🔒 로그인
앱에 처음 접속하면 로그인 화면이 나타납니다. 발급받은 **아이디**와 **비밀번호**를 입력하여 로그인합니다.

### 📁 데이터 업로드
1. 화면 왼쪽 사이드바에 있는 **「📁 데이터 파일 업로드」** 영역을 찾습니다.
2. 분석하고자 하는 데이터 파일(`.csv` 또는 `.xlsx`)을 드래그 앤 드롭하거나 클릭하여 업로드합니다.
3. 데이터가 성공적으로 업로드되면 **자동 분석**이 즉시 실행됩니다:
   - 📋 총 행/열 개수, 수치형/범주형/날짜형 변수 목록
   - ⚠️ 결측값이 있는 컬럼과 개수
   - 📅 날짜 형식으로 보이는 컬럼의 자동 변환
   - 💡 1:N 반복 구조 감지 (예: "환자ID 기준 평균 3.5행 반복")

---

## 2단계: 초기 데이터 설정

자동 분석이 끝나면, 본격적인 분석 전에 **데이터의 기준을 설정**하는 단계가 시작됩니다.

### ① 환자 ID · 종속변수 지정

| 설정 항목 | 설명 | 예시 |
|-----------|------|------|
| **🆔 환자 ID 컬럼** | 개인을 식별하는 고유 식별자. 설정하면 분석 변수에서 자동 제외됩니다. | `Patient_ID`, `등록번호` |
| **🎯 종속변수 컬럼** | 분석의 목표(결과) 변수. 회귀분석 등에서 Y변수로 자동 선택됩니다. | `발병여부`, `생존일수` |

- 종속변수가 **수치형이지만 실제로는 범주를 의미**하는 경우(예: 0/1 코드), **「🔄 범주형으로 변환」** 체크박스를 활성화하세요.
- 설정이 끝나면 **「✅ 다음 단계로」** 를 클릭합니다. 나중에 설정하려면 **「⏭️ 건너뛰기」** 도 가능합니다.

### ② 데이터 타입 확인 · 변환

각 컬럼의 데이터 타입을 확인하고 필요 시 변환하는 단계입니다.

- **💡 타입 변환 추천**: 앱이 데이터를 자동으로 스캔하여 범주형으로 변환하면 유리한 컬럼을 추천합니다.
  - **「✨ 추천 모두 적용하기」**: 추천된 변환을 한번에 일괄 적용
  - 개별 추천 옆의 **「🔄 변환」**: 하나씩 선택적으로 적용
- **수동 타입 변환**: 하단의 데이터 타입 표에서 **행을 클릭**하여 원하는 타입으로 직접 변경할 수 있습니다.
  - 선택 가능한 타입: `수치형 (float)` · `정수형 (int)` · `문자형 (object)` · `범주형 (category)` · `날짜형 (date)`
- **↩️ 되돌리기**: 잘못 변환했다면 변환 이력에서 **↩️ 버튼**을 눌러 원래 타입으로 복구할 수 있습니다.
- 모든 설정이 끝나면 **「✅ 설정 완료 → 분석 시작!」** 을 클릭합니다.

> 💡 설정 완료 후에도 사이드바의 **「⚙️ 설정 변경」** 버튼으로 언제든지 돌아올 수 있습니다.

---

## 3단계: 데이터 탐색 (기초 분석)

메인 화면에서 **「데이터 탐색」** 버튼을 클릭하면 9가지 기초 분석 도구를 사용할 수 있습니다.

### 📊 분포 / 이상치 분석
수치형 변수의 **히스토그램**과 **박스플롯**을 통해 데이터 분포 형태와 이상치를 확인합니다.
- 컬럼을 선택하거나, **선택하지 않으면 모든 수치형 컬럼을 한번에** 분석합니다.
- 평균, 중앙값, 표준편차, 왜도 등의 기술통계도 함께 제공됩니다.

### 🔗 상관관계 (피어슨)
수치형 변수들 간의 **피어슨 상관계수 히트맵**을 자동 생성합니다. 클릭 즉시 실행됩니다.

### ❓ 결측값 / 기술통계 분석
모든 컬럼의 **결측값 현황**(막대 그래프)과 **기술통계 요약**(왜도·첨도 포함)을 한번에 보여줍니다.

### 🔍 이상치 탐지
**IQR(1.5×IQR)** 과 **Z-score(|z|>3)** 두 가지 방법으로 이상치를 정밀 탐지합니다.
- 컬럼을 선택하거나, **선택하지 않으면 모든 수치형 컬럼을 한번에** 탐지합니다.

### 📂 빈도 분석
범주형 변수의 **빈도수와 비율**을 막대 그래프 + 파이 차트로 시각화합니다.
- 컬럼을 선택하거나, **선택하지 않으면 모든 범주형 컬럼을 한번에** 분석합니다.

### 🔗 스피어만 상관분석
비선형 관계도 잡아낼 수 있는 **스피어만 상관계수 히트맵**입니다. 순서형·비정규 데이터에 적합합니다.

### 📊 산점도 행렬
수치형 변수들의 **모든 조합에 대한 산점도**를 한눈에 볼 수 있는 행렬입니다. 대각선은 각 변수의 분포를 보여줍니다.

### 📐 다중공선성 확인 (VIF)
회귀분석 전에 반드시 확인해야 할 **분산 팽창 인수(VIF)** 를 계산합니다.
- VIF ≥ 10이면 해당 변수를 제거하는 것을 권장합니다.

### 🎻 바이올린 플롯
그룹별 데이터의 **분포 형태와 박스플롯**을 동시에 시각화합니다. 수치 컬럼과 그룹 기준 컬럼을 선택합니다.

---

## 4단계: 통계 검정 (고급 분석)

메인 화면에서 **「통계 검정」** 버튼을 클릭하면 11가지 통계 분석 도구를 사용할 수 있습니다.

### ⚖️ 두 그룹 비교 (T-test)
두 집단의 평균이 통계적으로 다른지 확인합니다.
- 정규성을 자동 확인하여, 정규분포이면 **독립 t검정**, 아니면 **Mann-Whitney U검정**으로 자동 전환합니다.
- Cohen's d 효과 크기도 함께 제공됩니다.
- **종속변수로 설정한 컬럼이 기본 선택**됩니다.

### 📊 여러 그룹 비교 (ANOVA)
3개 이상 그룹의 평균 차이를 한번에 검정합니다.
- **Tukey 사후검정**(Bonferroni 보정)이 자동 실행됩니다.
- 종속변수가 기본 선택됩니다.

### 📈 영향 분석 (Regression)
어떤 변수가 목표값에 얼마나 영향을 미치는지 **선형 회귀분석**으로 분석합니다.
- 예측 목표(Y)와 사용할 변수(X)를 선택합니다.
- R² 설명력과 회귀 계수가 결과로 제공됩니다.
- 종속변수가 Y의 기본 선택으로 설정됩니다.

### 🔀 카이제곱 검정 (Chi-squared Test)
두 **범주형 변수** 간의 독립성을 검정합니다. 교차표(Cross Table)와 함께 결과가 제공됩니다.

### 🔄 전후 비교 (Paired T-test)
같은 대상의 **전/후 수치**를 비교하는 대응표본 검정입니다.
- 정규성 미충족 시 **Wilcoxon 부호순위 검정**으로 자동 전환됩니다.

### 📊 Kruskal-Wallis
ANOVA의 **비모수 버전**입니다. 데이터가 정규분포를 따르지 않을 때 여러 그룹을 비교합니다.
- 종속변수가 기본 선택됩니다.

### 📊 비율 비교 (Proportion Test)
두 그룹 간 **특정 값의 비율 차이**를 검정합니다.

### 🎯 로지스틱 회귀 (Logistic Regression)
이진 결과(예/아니오, 0/1)를 예측하는 분석입니다.
- 2개 값만 가진 컬럼이 자동으로 Y 후보로 표시됩니다.
- 종속변수가 기본 선택됩니다.

### ⏱️ 생존분석 (Kaplan-Meier)
시간에 따른 **생존 확률 곡선**을 그립니다.
- 시간 컬럼(수치형)과 이벤트 컬럼(0/1)을 선택합니다.
- 선택적으로 그룹 비교(Log-rank 검정)도 가능합니다.

### 📉 Cox 회귀 분석 (Cox Regression)
여러 변수가 **생존에 미치는 영향**을 동시에 분석하는 비례위험 모델입니다.
- 위험비(HR)가 1보다 크면 위험 증가, 1보다 작으면 보호 효과를 의미합니다.

---

## 5단계: 데이터 펼치기 (피벗 변환)

메인 화면에서 **「데이터 펼치기」** 버튼을 클릭하면 2가지 피벗 모드를 사용할 수 있습니다.

### 📋 1:N으로 펼치기

한 환자가 여러 행에 걸쳐 반복된 데이터(예: 여러 번의 검사 기록)를 **한 행으로 가로 정리**합니다.

**사용 방법:**

1. **기준 컬럼** 선택: 같은 값끼리 묶을 컬럼 (예: 환자ID)
2. **펼칠 데이터 컬럼** 선택: 가로로 펼칠 컬럼들
3. **⚙️ 펼치기 옵션** 설정:

| 옵션 | 설명 |
|------|------|
| **📋 정렬 기준 컬럼** | 펼치기 전에 각 그룹 내 데이터를 정렬합니다. 예: 날짜 기준 오름차순 → `_1`이 가장 오래된 날짜 |
| **↕️ 정렬 방향** | 오름차순 (작은 값 → 큰 값) 또는 내림차순 |
| **🔀 컬럼 순서 인터리브** | 펼칠 컬럼이 2개 이상일 때 활성화됩니다 |

**인터리브 컬럼 순서란?**

펼칠 컬럼이 A, B 두 개일 때:

- **인터리브 OFF**: `A_1, A_2, A_3, B_1, B_2, B_3` (변수별로 묶음)
- **인터리브 ON**: `A_1, B_1, A_2, B_2, A_3, B_3` (같은 순번끼리 묶음)

> 💡 인터리브를 활성화하면 같은 시점의 데이터를 나란히 비교하기 편리합니다.

### 📊 집계하여 요약

데이터를 **sum·mean·count·min·max·first** 등의 집계 함수로 요약하는 전통적인 피벗 테이블입니다.

**사용 방법:**

1. **기준 컬럼 (행)**: 피벗 테이블의 행이 될 컬럼
2. **열 컬럼**: 피벗 테이블의 열이 될 컬럼 (선택 사항)
3. **값 컬럼 (숫자)**: 집계할 수치형 컬럼
4. **집계 방법**: sum, mean, count, min, max, first 중 선택
5. **⚙️ 정렬 옵션**: 피벗 전에 데이터를 원하는 기준으로 정렬할 수 있습니다

---

## 6단계: 결과 확인 및 내보내기

### 📝 대화형 결과 확인

KCDW 어시스턴트는 **챗봇 형식**으로 동작합니다.
- 버튼을 눌러 분석을 지시할 때마다 화면 하단에 **새로운 채팅과 함께 분석 결과**(표, 그래프)가 자동 스크롤되어 나타납니다.
- 이전에 수행했던 분석 기록은 **스크롤을 올려** 언제든 다시 확인할 수 있습니다.

### 📥 개별 결과 저장

각 분석 결과 아래에 다운로드 버튼이 제공됩니다:

| 버튼 | 형식 | 용도 |
|------|------|------|
| **📥 Excel 다운로드** | `.xlsx` | 데이터 테이블을 엑셀로 저장 |
| **📥 CSV 다운로드** | `.csv` | 다른 프로그램에서 활용 |
| **🖼️ PNG 이미지 저장** | `.png` | 차트를 고화질 이미지로 저장 (보고서·논문 삽입용) |

### 📋 전체 결과 일괄 저장

여러 분석을 수행한 후, 사이드바에서 **모든 결과를 한번에** 내보낼 수 있습니다:

| 버튼 | 설명 |
|------|------|
| **📋 전체 분석 Excel 저장** | 모든 분석 결과 테이블을 **다중 시트 Excel 파일**로 저장 |
| **🖼️ 전체 분석 이미지 저장 (ZIP)** | 모든 차트 이미지를 **PNG로 묶어 ZIP 파일**로 저장 |

> ⚠️ **PNG 저장이 안 되는 경우**: `kaleido` 패키지가 설치되어 있지 않으면 이미지 버튼이 표시되지 않습니다. `pip install kaleido==0.2.1`로 설치하세요.

---

## ⚡ 사이드바 빠른 분석

데이터 설정 완료 후 사이드바에 **빠른 분석 바로가기** 버튼들이 나타납니다. 메인 메뉴를 거치지 않고 한 번의 클릭으로 원하는 분석에 바로 진입할 수 있습니다:

- 📊 분포 확인
- 🔗 상관관계
- ❓ 결측/요약
- ⚖️ 두 그룹 비교(T-test)
- 📊 여러 그룹 비교(ANOVA)
- 📈 회귀분석
- 📐 데이터 펼치기

---

## 💡 유용한 팁

### 네비게이션
- **「🏠 홈으로 가기」**: 언제든 최상위 메뉴(데이터 탐색 / 통계 검정 / 데이터 펼치기)로 돌아갑니다.
- **「🔁 다시 설정」**: 현재 분석의 파라미터를 다시 선택할 수 있습니다.
- **「⚙️ 설정 변경」** (사이드바): 환자 ID, 종속변수, 데이터 타입 설정을 변경합니다.

### 종속변수 활용
- 종속변수를 설정하면 T-test, ANOVA, 회귀분석, Kruskal-Wallis, 로지스틱 회귀에서 **비교 대상/목표 변수가 자동으로 선택**되어 매번 찾아 선택할 필요가 없습니다.
- 종속변수는 분석의 편의를 위한 **기본값 설정 용도**이며, 실행 시 다른 컬럼으로 변경할 수 있습니다.

### 전체 분석 워크플로우 예시

```
1. 데이터 업로드 (CSV/Excel)
2. 환자 ID · 종속변수 설정
3. 데이터 타입 최적화 (추천 적용)
4. 데이터 탐색 → 분포 확인, 결측값 체크
5. 통계 검정 → T-test, ANOVA 등 수행
6. 전체 분석 Excel 저장 + 이미지 ZIP 저장
```

### 주의사항
- 새 파일을 업로드하면 이전 분석 기록이 **모두 초기화**됩니다.
- 분석 결과는 세션 내에서만 유지되며, 브라우저를 닫으면 사라집니다. 중요한 결과는 반드시 **다운로드**해 두세요.
- 대용량 데이터(수만 행 이상)의 경우 일부 분석(산점도 행렬, 생존분석 등)에 시간이 걸릴 수 있습니다.

---

지금 바로 파일을 업로드하고 첫 번째 분석을 시작해 보세요! 🎉
